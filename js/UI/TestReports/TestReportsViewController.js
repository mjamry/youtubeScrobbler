//namespace
window.UI = window.UI || {};

window.UI.TestReportsViewController = function(config)
{
    this._config = config;
};

window.UI.TestReportsViewController.prototype =
{
    initialise: function()
    {

    }
}
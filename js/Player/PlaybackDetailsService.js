//using
window.Player = window.Player || {};

window.Player.PlaybackDetailsService = function(){};

window.Player.PlaybackDetailsService.prototype =
{
    initialise: function()
    {
        //hook up to player events - timeupdate, started/ended
    }
};